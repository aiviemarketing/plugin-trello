{% block _config_trello_config_widget %}
<h4 class="fw-sb mt-48 mb-xs">{{ 'mautic.config.tab.trello_config'|trans }}</h4>
<div class="row">
    {% for f in form.children %}
        <div class="col-md-6">{{ form_row(f) }}</div>
    {% endfor %}
</div>
{% endblock %}
